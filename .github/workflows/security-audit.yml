name: 🛡️ Zero Trust Security Audit
on:
  push:
    branches: [ "main", "master" ]
  schedule:
    - cron: "0 0 * * 1" # Hetente egyszer hétfőn is lefut magától

jobs:
  vulnerability-scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Run Trivy Scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: "fs"
          scan-ref: "."
          format: "table"
          exit-code: "0"
          severity: "CRITICAL,HIGH"
